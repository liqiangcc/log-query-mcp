# 从预研原型进入正式实现检查清单

## 范围

- [ ] `REQUIREMENTS.md` 已同步真实行为。
- [ ] 明确 `newest_first` 首期取舍。
- [ ] 明确目录 glob 是否首期实现。
- [ ] 时间范围固定为 `[start, end)`。
- [ ] ASCII 大小写语义写入接口说明。
- [ ] match_ref 和 cursor 重启失效语义写入接口说明。

## 接口

- [ ] MCP 工具输入输出 Schema 冻结。
- [ ] JSON 配置 Schema 版本化。
- [ ] 错误类别冻结。
- [ ] 客户端只能缩小资源限制。
- [ ] 完整 JSON 响应大小有硬限制。

## 安全

- [ ] Linux kernel >= 5.6 检查为启动或部署硬门槛。
- [ ] `openat2()` 是唯一日志文件打开路径。
- [ ] 路径穿越、软链接、magic link 测试通过。
- [ ] 普通文件检查通过。
- [ ] 非 root 和只读日志权限实测。
- [ ] `RESOLVE_NO_XDEV` 已决策。
- [ ] 依赖安全审计加入 CI。

## 稳定性

- [ ] 扫描、排队和响应资源限制全部生效。
- [ ] 客户端断连能够取消底层扫描。
- [ ] 日志追加、轮转、截断和删除压力测试通过。
- [ ] match_ref/cursor TTL 和容量指标可观测。
- [ ] SIGTERM 优雅停止通过。

## 性能

- [ ] 1 GiB 单文件基准完成。
- [ ] 10 GiB 单文件基准完成。
- [ ] 10,000 小文件基准完成。
- [ ] 1、4、8 并发基准完成。
- [ ] 记录 CPU、RSS、吞吐、首条结果延迟和 P99。
- [ ] 冷/热缓存条件明确。
- [ ] 默认资源限制根据实测调整。

## MCP 兼容性

- [ ] MCP Inspector 验证。
- [ ] 实际 AI 客户端验证。
- [ ] stdio 协议烟测通过。
- [ ] Streamable HTTP 协议烟测通过。
- [ ] 会话创建、后续请求和删除通过。
- [ ] SDK 升级回归门禁已建立。

## 部署

- [ ] systemd unit 评审。
- [ ] CPU、内存、FD、任务数限制实测。
- [ ] 配置文件和日志目录权限检查通过。
- [ ] 升级和回滚步骤完成。
- [ ] 监控和日志字段定义完成。

所有 P0 安全项和目标环境验收项完成后，预研结论才能从 CONDITIONAL GO 转为生产 GO。
