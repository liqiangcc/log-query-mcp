# 日志查询 MCP 需求文档

> 版本：v1.1  
> 阶段：首期正式实现  
> 定位：面向 AI 问题排查的只读日志搜索服务  
> 接口基线：[MCP_API_V1.md](./docs/MCP_API_V1.md)  
> 配置基线：[CONFIG_SCHEMA_V1.md](./docs/CONFIG_SCHEMA_V1.md)

---

## 1. 项目简介

日志查询 MCP 是一个部署在 Linux 服务器上的只读日志搜索服务。

本地 AI 读取代码仓库，通过 MCP 查询服务器运行日志，将代码上下文与日志上下文结合，用于定位开发环境或测试环境中的代码、数据、配置和运行问题。

```text
本地 AI
├── 读取本地代码仓库
└── 调用日志查询 MCP
          ↓
    白名单服务器日志
```

日志查询 MCP 只负责安全搜索、分页和有限上下文读取，不负责分析代码、判断根因或生成修复代码。

---

## 2. 背景

人工登录服务器后使用 `grep`、`tail` 等命令存在以下问题：

- AI 不知道日志位置和服务对应关系。
- 不同服务的日志目录和文件命名方式不一致。
- 每次查询都需要人工执行命令。
- 完整日志量大，直接返回会浪费时间和 token。
- 直接开放 Shell 或服务器文件系统权限风险过高。
- 一次请求可能经过多个服务，人工关联效率低。
- 单个匹配行通常不足以理解异常堆栈和业务过程。

因此需要一个基于日志来源白名单、严格资源限制和只读文件权限的 MCP 服务。

---

## 3. 建设目标

首期系统应实现：

1. 管理员集中配置允许查询的日志来源。
2. AI 查询当前已启用的日志来源。
3. AI 按字面量关键字搜索一个或多个来源。
4. 支持 `requestId`、`traceId`、异常名、错误码、接口路径和业务 ID。
5. 支持 RFC 3339 时间范围。
6. 返回来源、文件标识、行号、匹配内容和可选时间戳。
7. 通过短期 `match_ref` 读取有限前后文。
8. 通过短期 cursor 获取后续结果。
9. 限制来源数、文件数、扫描字节、执行时间、结果数、响应大小和并发。
10. 禁止客户端提交服务器路径或执行任意命令。
11. 支持 Linux 非 root、只读权限和 systemd 部署。
12. 为正式验收提供协议、安全、性能和部署测试。

---

## 4. 核心原则

### 4.1 日志来源白名单

管理员负责配置来源根目录、显式文件和目录发现规则。AI 只能提交 `source_id`，不能提交服务器文件路径、目录路径或文件名规则。

### 4.2 只读搜索

服务不允许修改、创建、删除、移动、清空或截断日志文件，不提供 Shell 工具和通用文件浏览能力。

### 4.3 小结果、按需展开

`search_logs` 返回少量候选匹配；AI 只对有价值的结果调用 `get_log_context`，避免一次返回大量日志。

### 4.4 受控内网

首期仅在受控内网部署，不内置：

- MCP 客户端认证。
- 日志来源级用户授权。
- MCP 自身 TLS。

网络层负责限制谁能连接；MCP 服务负责限制连接后能够读取什么。

### 4.5 单实例首期

首期采用单实例部署。`match_ref` 和 cursor 保存在服务进程内存中，服务重启后失效。

---

## 5. 职责划分

### 5.1 本地 AI

负责：

- 读取本地代码仓库。
- 根据用户问题定位相关模块。
- 调用 MCP 工具。
- 从日志提取新的 `traceId`、业务 ID 和异常线索。
- 结合代码和日志分析问题。
- 输出原因假设、证据和排查建议。

### 5.2 日志查询 MCP

负责：

- 加载和验证管理员配置。
- 安全打开白名单日志文件。
- 执行字面量搜索、时间过滤和稳定排序。
- 管理分页 cursor 和上下文 `match_ref`。
- 强制执行资源限制。
- 返回去敏后的稳定错误。

### 5.3 管理员

负责：

- 配置日志来源、服务、环境和标签。
- 配置显式文件和可选目录规则。
- 配置日志时间戳规则。
- 保证运行账号对日志只读、对配置不可写。
- 保证服务只在受控内网可访问。
- 确认日志内容适合返回给当前内网 AI 客户端。

---

## 6. 平台和部署要求

### 6.1 平台

- 操作系统：Linux。
- 内核版本：`>= 5.6`。
- 正式传输：MCP Streamable HTTP。
- 本机调试：stdio。
- 运行账号：独立非 root 用户。

低于 Linux 5.6 的环境不得静默退化为字符串前缀或一次性 `realpath` 路径校验。

### 6.2 网络

- 默认监听回环地址。
- 监听内网地址必须显式配置。
- 防火墙、安全组或反向代理负责网络访问范围。

### 6.3 文件权限

- 日志目录建议只读挂载。
- 配置文件不得由服务运行账号修改。
- 非可信用户不得在日志来源目录创建文件或硬链接。

### 6.4 配置加载

- v1 使用版本化 JSON 配置，`version=1`。
- 启动时整体解析和验证。
- 任一关键来源配置错误时拒绝启动。
- v1 通过重启加载配置，不要求热更新。

---

## 7. 日志来源配置

### FR-01 来源字段

每个来源至少包含：

| 字段 | 必填 | 说明 |
|---|---:|---|
| `source_id` | 是 | 全局唯一 ASCII 标识 |
| `name` | 是 | 面向 AI 的显示名称 |
| `description` | 否 | 来源说明 |
| `service` | 是 | 服务或模块名 |
| `environment` | 是 | 环境名 |
| `tags` | 否 | 辅助选择标签 |
| `enabled` | 否 | 默认 `true` |
| `encoding` | 否 | v1 固定为 `utf-8` |
| `root` | 是 | 管理员配置的 Linux 绝对目录 |
| `files` | 条件必填 | 相对 root 的显式普通文件 |
| `directories` | 条件必填 | 安全目录发现规则 |
| `timestamp_rule` | 否 | 来源级时间戳解析规则 |

`files` 和 `directories` 至少有一个非空。

### FR-02 显式文件

- 文件路径必须是相对 `root` 的规范化路径。
- 不允许空路径、绝对路径、`.` 或 `..` 组件。
- 启动时验证文件能够安全打开且为普通文件。
- 查询时必须重新执行安全打开，不得只依赖启动时验证。

### FR-03 目录发现

v1 目录规则包含：

```json
{
  "path": "archive",
  "recursive": false,
  "include_suffixes": [".log", ".log.1"]
}
```

规则：

- `path` 为 `.` 或规范化相对目录。
- `recursive` 默认 `false`。
- 文件名使用管理员配置的区分大小写后缀白名单。
- 不允许 AI 提交目录或后缀。
- 不跟随软链接。
- 只发现普通文件。
- 发现数量有硬上限。
- 结果按相对路径稳定排序。

### FR-04 日志轮转

查询采用尽力而为一致性：

- 不锁定日志文件。
- 不保证查询期间完整快照。
- 日志追加、轮转、删除或截断不得导致服务崩溃。
- 文件身份变化时，相关 cursor 或 `match_ref` 失效。
- 当前日志和未压缩轮转日志可以作为候选文件。

---

## 8. MCP 工具

首期提供三个工具：

| 工具 | 作用 |
|---|---|
| `list_log_sources` | 查询已启用日志来源 |
| `search_logs` | 搜索一个或多个来源 |
| `get_log_context` | 读取匹配位置有限上下文 |

机器可读契约见 [`schemas/mcp-tools-v1.schema.json`](./schemas/mcp-tools-v1.schema.json)。

### FR-05 `list_log_sources`

请求：

```json
{}
```

响应来源包含：

```text
source_id
name
description
service
environment
tags
```

不得返回根目录、文件列表、配置路径、操作系统用户或凭证。

### FR-06 `search_logs`

请求：

```json
{
  "source_ids": ["payment-test", "order-test"],
  "keyword": "traceId=abc123",
  "case_sensitive": false,
  "start_time": "2026-06-19T14:00:00+09:00",
  "end_time": "2026-06-19T15:00:00+09:00",
  "order": "oldest_first",
  "max_results": 50,
  "cursor": null
}
```

参数约束：

| 参数 | 必填 | v1 约束 |
|---|---:|---|
| `source_ids` | 是 | 1–10 个，唯一，来自 `list_log_sources` |
| `keyword` | 是 | 1–256 字符，不含换行 |
| `case_sensitive` | 否 | 默认 `false` |
| `start_time` | 否 | RFC 3339，包含边界 |
| `end_time` | 否 | RFC 3339，不包含边界 |
| `order` | 否 | v1 只接受 `oldest_first` |
| `max_results` | 否 | 默认 50，范围 1–200 |
| `cursor` | 否 | 服务端生成的短期 token |

### FR-07 搜索语义

- 支持 UTF-8 中文和英文。
- 使用字面量子串匹配。
- 不解释为正则、glob、Shell 或查询语言。
- 行号从 1 开始。
- `case_sensitive=false` 仅保证 ASCII 大小写折叠。
- 非 ASCII 内容按 UTF-8 字节精确匹配。
- 路径样式关键字只作为日志内容，不影响文件选择。

### FR-08 时间范围

查询区间固定为：

```text
[start_time, end_time)
```

即开始包含、结束不包含。

来源配置时间规则时：

- 解析日志行时间。
- 对可解析结果执行时间过滤。
- 返回 RFC 3339 `timestamp`。

时间无法解析或明显畸形时：

- 不伪造时间。
- 匹配结果保守返回。
- `timestamp=null`。

文件修改时间只用于保守候选筛选或文件排序，不作为单条事件时间返回。

### FR-09 结果排序

v1 只支持：

```text
oldest_first
```

排序规则：

```text
可解析绝对时间
→ 来源请求顺序
→ 文件稳定顺序
→ 行号
```

无时间结果位于可解析时间结果之后。跨服务排序反映日志记录时间，不保证真实因果顺序。

`newest_first` 为后续可选能力，不属于 v1 验收范围。

### FR-10 搜索结果

每条结果包含：

```text
match_ref
source_id
file_id
file_name
line_number
timestamp
content
content_truncated
```

响应包含：

```text
results
truncated
next_cursor
```

规则：

- `file_name` 只能是文件名或来源内相对显示名。
- 不返回服务器绝对路径。
- 超长行返回有限预览并设置 `content_truncated=true`。
- 无匹配返回空 `results`，不是工具错误。

### FR-11 分页 cursor

- cursor 由服务端生成，对客户端不透明。
- cursor 绑定规范化查询条件、候选文件快照、扫描位置和累计资源使用。
- 后续页必须保持 `source_ids`、关键字、大小写、时间范围、顺序和 `max_results` 不变。
- 每次成功返回下一页时生成新的 cursor。
- cursor 具有 TTL 和容量上限。
- 服务重启、过期、淘汰、文件变化或条件不一致时拒绝使用。
- 当结果被截断但无法安全继续时，允许 `truncated=true` 且 `next_cursor=null`。

### FR-12 `get_log_context`

请求：

```json
{
  "match_ref": "mref_...",
  "before_lines": 10,
  "after_lines": 30
}
```

约束：

- 只能使用 `search_logs` 返回的 `match_ref`。
- 不接受路径、`file_id`、任意行号或字节偏移。
- 单侧最多 50 行。
- 上下文内容计入完整响应限制。
- 文件轮转、替换、删除、截断或引用失效时返回工具错误。
- 不提供完整文件下载。

响应包含：

```text
source_id
file_id
file_name
start_line
end_line
lines[] { line_number, content }
truncated
```

首期不要求自动识别完整多行事件；AI 通过请求足够的后续行获取异常堆栈和 `Caused by`。

---

## 9. 错误模型

业务错误使用 MCP 工具错误结果，首个文本块为紧凑 JSON：

```json
{
  "code": "UNKNOWN_SOURCE",
  "message": "one or more requested log sources are unavailable",
  "retryable": false
}
```

稳定错误代码：

```text
INVALID_ARGUMENT
UNKNOWN_SOURCE
SOURCE_UNAVAILABLE
DEADLINE_EXCEEDED
QUERY_CANCELLED
RESOURCE_LIMIT
CURSOR_INVALID
MATCH_REF_INVALID
FILE_CHANGED
INTERNAL_ERROR
```

完整映射见 [`docs/ERROR_MODEL_V1.md`](./docs/ERROR_MODEL_V1.md)。

错误消息不得包含服务器绝对路径、底层系统调用参数、Rust backtrace、配置路径或凭证。

---

## 10. 资源限制

v1 默认值：

| 限制 | 默认值 |
|---|---:|
| 单次最大来源数 | 10 |
| 关键字最大长度 | 256 字符 |
| 单次最大扫描文件数 | 500 |
| 单页最大扫描字节数 | 512 MiB |
| 查询 deadline | 10 秒 |
| 默认单页结果数 | 50 |
| 单页结果硬上限 | 200 |
| 单条返回内容 | 16 KiB |
| 返回内容合计 | 512 KiB |
| 完整 MCP 响应 | 1 MiB |
| 单侧上下文行数 | 50 |
| 同时扫描任务数 | 4 |
| `match_ref` TTL | 10 分钟 |
| cursor TTL | 5 分钟 |

服务端必须限制：

- 来源数。
- 扫描文件数。
- 扫描字节数。
- 排队和执行时间。
- 结果数和内容字节数。
- 完整序列化响应大小。
- 上下文扫描和行数。
- 并发扫描任务。
- cursor 和 `match_ref` 状态容量。

客户端参数只能缩小或在服务端允许范围内选择，不能突破服务端生效上限。

---

## 11. 安全约束

系统必须满足：

1. 只能读取管理员配置的日志来源。
2. 客户端不能提交服务器路径或目录。
3. 禁止路径穿越。
4. 禁止跟随软链接和 magic link。
5. v1 禁止从来源 root 跨越到嵌套挂载点。
6. 只能读取普通文件。
7. 禁止读取目录、设备文件、FIFO 和 Socket。
8. 服务使用非 root 账号运行。
9. 日志目录对运行账号只读。
10. 配置文件对运行账号不可写。
11. 禁止执行 Shell 和外部搜索命令。
12. 错误响应不得泄露绝对路径、堆栈和凭证。
13. 日志内容被视为不可信数据，不作为 MCP 指令执行。
14. 自动敏感信息脱敏不属于首期；管理员必须审计允许查询的来源。

实际文件打开必须使用：

```text
openat2
+ RESOLVE_BENEATH
+ RESOLVE_NO_SYMLINKS
+ RESOLVE_NO_MAGICLINKS
+ RESOLVE_NO_XDEV
+ fstat 普通文件校验
```

跨挂载策略见 [`docs/adr/0006-enable-resolve-no-xdev-in-v1.md`](./docs/adr/0006-enable-resolve-no-xdev-in-v1.md)。

---

## 12. 非功能需求

### 12.1 稳定性

- 单个异常行、非法 UTF-8 或超长行不得导致进程退出。
- 日志追加、轮转、截断、删除不得导致进程崩溃。
- 达到资源限制后查询应停止。
- 客户端取消或断开后，应尽快取消对应排队或扫描任务。
- 并发查询不得无限增加线程、内存、FD 或磁盘 I/O。
- SIGTERM 应优雅停止服务。

### 12.2 一致性

- 查询不锁定日志文件。
- 不承诺查询期间完整快照。
- 对正在写入或轮转的日志采用尽力而为读取。
- cursor 和 `match_ref` 绑定文件身份，文件变化后安全失效。
- 无法解析时间时不承诺精确跨服务时间线。

### 12.3 可观测性

服务至少记录：

- 工具名称。
- 来源 ID 集合。
- 查询、排队和扫描耗时。
- 扫描文件数和字节数。
- 返回结果数和响应大小。
- 停止原因。
- cursor / `match_ref` 创建和失效数量。
- deadline、取消和资源限制次数。
- 工具错误代码。

默认不记录完整关键字，可以记录长度或不可逆摘要。

---

## 13. 边界说明

首期不负责：

- 读取或分析本地代码仓库。
- 判断根因或生成修复代码。
- 采集、同步或管理日志轮转。
- 实时 tail/follow。
- 压缩日志。
- 正则表达式或复杂查询语言。
- 完整日志下载。
- Kubernetes、Loki 或 Elasticsearch。
- 多实例共享 token。
- 客户端认证、来源授权或 TLS。
- 自动敏感信息脱敏。
- `newest_first` 实际扫描。

---

## 14. 首期验收标准

### 14.1 功能验收

1. 服务能部署在 Linux kernel 5.6 及以上服务器。
2. 能加载 `version=1` JSON 配置。
3. 能配置至少一个显式文件来源和一个目录规则来源。
4. `list_log_sources` 不返回绝对路径。
5. `search_logs` 支持中文和英文普通字符串。
6. 搜索内容不被解释为正则、glob 或 Shell。
7. `case_sensitive=false` 的 ASCII 大小写行为与文档一致。
8. 能搜索单个和多个来源。
9. 能返回来源、文件标识、显示名、行号和内容。
10. 时间区间按 `[start_time, end_time)` 过滤。
11. 未知或畸形日志时间不被伪造。
12. v1 `oldest_first` 返回稳定结果。
13. `newest_first` 请求返回 `INVALID_ARGUMENT`。
14. 无匹配返回空数组。
15. 结果过多时设置 `truncated=true`。
16. 可继续时返回 `next_cursor`。
17. cursor 条件变化时拒绝续查。
18. `get_log_context` 返回有限前后文和行号。
19. 能获取异常堆栈后续行。
20. 超长行和响应大小限制明确生效。

### 14.2 安全验收

21. 工具接口不接受服务器路径。
22. 未配置文件无法查询。
23. `../`、绝对路径和编码后的路径样式文本不能改变文件范围。
24. 最终和中间软链接均被拒绝。
25. magic link 被拒绝。
26. 嵌套 mount 或 bind mount 跨越被 `RESOLVE_NO_XDEV` 拒绝。
27. 文件在配置验证后被替换为软链接时仍不能读取目标。
28. 目录、设备文件、FIFO 和 Socket 不被读取。
29. 非 root 运行账号不能修改日志和配置。
30. 工具参数不能执行 Shell。
31. 错误响应不包含绝对路径、堆栈或凭证。

### 14.3 稳定性和协议验收

32. stdio 和 Streamable HTTP 三工具协议烟测通过。
33. MCP Inspector 和至少一个实际 AI 客户端通过。
34. 日志追加、轮转、截断和删除时服务不崩溃。
35. 达到文件、字节、结果、响应或时间限制后查询停止。
36. 达到并发上限后资源使用保持有界。
37. 客户端取消或断开后对应扫描在目标时限内退出。
38. `match_ref` 和 cursor 过期、重启和文件变化语义通过测试。
39. SIGTERM 优雅停止通过。

### 14.4 性能和部署验收

40. 完成 1 GiB 和 10 GiB 单文件基准。
41. 完成 10,000 小文件基准。
42. 完成 1、4、8 并发 CPU、RSS、吞吐和 P99 数据采集。
43. systemd 的 CPU、内存、FD 和任务数限制实测通过。
44. 默认资源限制根据目标服务器数据确认或调整。

---

## 15. 后续可选能力

- `newest_first` 反向扫描。
- 更丰富的目录 glob。
- JSON 结构化字段查询。
- 多关键字 AND、OR 和排除条件。
- 正则表达式。
- 压缩日志。
- 实时 tail。
- 多实例共享 cursor / `match_ref`。
- Loki、Elasticsearch 或 Kubernetes 适配器。
- 自动敏感信息脱敏。
- 客户端认证和来源授权。

---

## 16. 核心定义

> 日志查询 MCP 是一个基于日志来源白名单的受控文本搜索服务，不是远程文件浏览器或命令执行器。

人工负责配置服务器日志来源；日志查询 MCP 负责安全搜索、分页和有限上下文；AI 结合本地代码仓库和结果完成问题诊断。
