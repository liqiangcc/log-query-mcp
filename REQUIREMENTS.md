# 日志查询 MCP 需求文档

> 版本：v1.0  
> 阶段：首期  
> 定位：面向 AI 问题排查的只读日志搜索服务

---

## 1. 项目简介

日志查询 MCP 是一个部署在服务器上的只读日志搜索服务。

AI 在本地读取代码仓库，并通过 MCP 查询服务器上的运行日志，将代码上下文与日志上下文结合，用于定位和诊断开发环境或测试环境问题。

```text
本地 AI
├── 读取本地代码仓库
└── 调用日志查询 MCP
          ↓
    服务器日志文件
```

日志查询 MCP 只负责安全、受控地搜索日志并返回结果，不负责分析代码、判断根因或生成修复方案。

---

## 2. 背景

AI 排查问题时通常只能读取本地源码，缺少服务器运行时日志，因此难以判断问题是由代码、数据、配置还是运行环境引起。

当前日志查询通常依赖人工登录服务器，再使用 `grep`、`tail` 等命令搜索，存在以下问题：

- AI 不知道日志文件的位置。
- 不同服务的日志目录和文件命名方式不一致。
- 每次查询都需要人工执行命令。
- 完整日志内容较多，直接提供给 AI 会浪费时间和 token。
- 直接向 AI 开放 Shell 或服务器文件系统权限存在安全风险。
- 一次请求可能经过多个服务，人工跨服务关联日志效率较低。
- 仅返回匹配行通常不足以分析异常堆栈和前后业务过程。

因此需要一个受控的日志查询 MCP，为 AI 提供统一的日志发现、搜索和上下文读取能力。

---

## 3. 建设目标

系统应实现：

1. 在服务器上集中配置允许查询的日志文件和目录。
2. AI 可以查询当前可用的日志来源。
3. AI 可以按普通字符串搜索一个或多个日志来源。
4. 支持通过 `requestId`、`traceId`、异常名、错误码、接口路径和业务 ID 等内容查询日志。
5. 支持限定问题发生的大致时间范围。
6. 返回日志来源、文件标识、行号和匹配内容。
7. AI 可以按需读取匹配位置前后的有限日志上下文。
8. 支持跨服务搜索同一个关联标识，帮助还原请求链路。
9. 结果过多时能够截断，并允许继续查询后续结果。
10. 限制扫描范围、执行时间和返回大小。
11. 不向 AI 开放任意命令执行和任意文件读取权限。
12. AI 结合本地代码仓库和日志结果完成问题诊断。

---

## 4. 核心原则

### 4.1 日志源白名单

人工负责配置服务器上允许查询的日志文件和目录。

AI 只能选择已配置的日志来源，不能提交服务器文件路径、目录路径或文件匹配表达式。

### 4.2 只读搜索

MCP 只允许读取日志，不允许修改、删除、移动、清空或创建服务器文件。

### 4.3 面向排查问题

功能围绕以下排查链路设计：

```text
发现日志来源
    ↓
搜索问题线索
    ↓
缩小时间和服务范围
    ↓
查看匹配位置上下文
    ↓
关联其他服务日志
    ↓
结合本地代码定位问题
    ↓
修改或部署后再次搜索验证
```

### 4.4 小结果、按需展开

搜索接口优先返回少量候选匹配行。

AI 只对有价值的匹配结果继续读取上下文，避免一次返回大量日志。

### 4.5 内网部署前提

本服务仅部署和使用于受控内网环境，由防火墙、安全组或其他网络访问策略限制访问范围。

首期不提供：

- MCP 客户端身份认证。
- 日志来源级用户授权。
- MCP 自身的 TLS 或其他传输加密能力。

网络层负责限制谁能够连接；MCP 自身负责限制连接后能够读取什么和执行什么。

---

## 5. 职责划分

### 5.1 本地 AI

负责：

- 读取本地代码仓库。
- 根据用户描述定位相关模块。
- 调用日志查询 MCP。
- 从日志中提取新的线索，例如 `traceId`、业务 ID 和异常名。
- 结合代码与日志分析问题。
- 输出可能原因、证据和排查建议。
- 修改代码后，在相应代码已经部署或运行于测试环境时查询日志进行验证。

### 5.2 日志查询 MCP

负责：

- 读取人工配置的日志来源。
- 搜索日志文本。
- 返回匹配位置和有限上下文。
- 控制扫描时间、数据量和响应大小。
- 防止访问配置范围之外的文件。
- 防止任意命令执行和文件修改。

### 5.3 人工管理员

负责：

- 配置日志文件和目录。
- 配置服务、环境和标签信息。
- 配置允许查询的文件名规则。
- 配置可选的日志时间戳解析规则。
- 确保 MCP 运行账号对日志只有只读权限。
- 确保服务仅在受控内网中可访问。

---

## 6. 典型使用流程

```text
用户向 AI 描述问题
        ↓
AI 在本地代码仓库中定位相关模块
        ↓
AI 查询可用日志来源
        ↓
AI 根据服务、环境和时间选择日志来源
        ↓
AI 使用 requestId、traceId、异常名或业务 ID 搜索日志
        ↓
MCP 返回匹配文件、行号和内容
        ↓
AI 对关键匹配读取前后上下文
        ↓
AI 从上下文中提取新的关联标识并继续跨服务搜索
        ↓
AI 结合代码路径和日志时间线分析问题
        ↓
AI 输出可能原因、证据和排查建议
```

MCP 只负责日志搜索，不负责分析问题。

---

## 7. 典型使用场景

### 7.1 根据请求 ID 查询日志

用户提供：

```text
requestId=abc123
```

AI 在相关服务日志中搜索该请求 ID，查看请求进入、处理、调用下游和返回结果的日志，并结合本地代码分析执行路径。

### 7.2 根据 traceId 关联多个服务

一次请求可能经过：

```text
gateway
order-service
payment-service
message-service
```

AI 使用同一个 `traceId` 同时搜索多个日志来源，并按时间查看结果，帮助还原跨服务调用过程。

### 7.3 根据异常名称定位代码

用户提供：

```text
PaymentAuthException
```

AI 先搜索异常日志，再读取匹配位置后的堆栈上下文，随后在本地代码仓库中搜索异常定义、抛出位置和调用方。

### 7.4 根据业务标识查询日志

支持搜索：

- `orderId`
- `userId`
- `taskId`
- `messageId`
- 其他业务唯一标识

AI 根据日志中的业务状态、关联请求 ID 和本地业务代码判断问题原因。

### 7.5 根据大致时间排查问题

用户可能只知道：

```text
今天 14:20 左右支付失败
```

AI 选择支付服务测试环境日志，将搜索范围限定在问题发生前后的一段时间，再搜索接口路径、错误级别、异常名或业务标识。

### 7.6 查看异常前后上下文

匹配行可能只有：

```text
PaymentAuthException
```

真正原因可能位于前后日志中，例如：

- 请求参数。
- 下游响应状态。
- 重试过程。
- 异常堆栈。
- `Caused by` 信息。
- 业务状态变更。

AI 对匹配结果调用上下文读取工具，获取有限的前后日志。

### 7.7 验证代码修改

AI 修改本地代码并完成构建或测试后，在修改后的代码已经部署或运行于相应测试环境时，查询部署后的时间范围，检查异常是否仍然出现。

---

## 8. 功能需求

## 8.1 日志来源配置

### FR-01 日志来源定义

每个日志来源至少包含：

| 字段 | 必填 | 说明 |
|---|---:|---|
| `source_id` | 是 | 日志来源唯一标识 |
| `name` | 是 | 面向 AI 的显示名称 |
| `description` | 否 | 日志来源说明 |
| `service` | 是 | 对应服务或模块名称 |
| `environment` | 是 | 环境，例如 `dev`、`test` |
| `tags` | 否 | 用于辅助选择日志来源的标签 |
| `paths` | 是 | 一个或多个日志文件或目录，由管理员配置 |
| `recursive` | 是 | 是否递归查询子目录 |
| `include_patterns` | 是 | 允许查询的文件名规则，由管理员配置 |
| `enabled` | 是 | 是否启用 |
| `encoding` | 否 | 文件编码，首期默认 UTF-8 |
| `timestamp_rule` | 否 | 可选的日志时间戳解析规则 |

### FR-02 文件范围

首期支持：

- 单个普通文本日志文件。
- 包含普通文本日志的目录。
- 未压缩的轮转日志。
- 管理员配置的文件名匹配规则，例如 `*.log`、`*.log.1`、`application-*.log`。

首期不支持：

- 压缩日志，例如 `.gz`、`.zip`。
- 二进制日志。
- 设备文件、FIFO、Socket 等特殊文件。
- 由 AI 提交文件名匹配规则。
- 由 AI 提交任意文件或目录路径。

### FR-03 日志轮转

查询过程中日志可能发生追加、重命名、轮转、截断或删除。

服务应采用尽力而为的一致性模型：

- 不锁定日志文件。
- 不保证查询期间形成完整快照。
- 文件在扫描期间被轮转或删除时，服务进程不得崩溃。
- 当前日志和未压缩轮转日志均可作为候选文件。

---

## 8.2 查询日志来源

### FR-04 `list_log_sources`

MCP 应提供：

```text
list_log_sources()
```

返回所有已启用的日志来源。

每个来源至少包含：

```json
{
  "source_id": "payment-test",
  "name": "支付服务测试环境",
  "description": "payment-service 应用日志",
  "service": "payment-service",
  "environment": "test",
  "tags": ["payment", "java"]
}
```

不得返回：

- 服务器绝对路径。
- 操作系统用户名。
- 配置文件位置。
- 服务器凭证。
- 与日志搜索无关的系统信息。

---

## 8.3 搜索日志

### FR-05 `search_logs`

MCP 应提供：

```text
search_logs(...)
```

建议请求结构：

```json
{
  "source_ids": ["payment-test", "order-test"],
  "keyword": "traceId=abc123",
  "case_sensitive": false,
  "start_time": "2026-06-19T14:00:00+09:00",
  "end_time": "2026-06-19T15:00:00+09:00",
  "order": "oldest_first",
  "max_results": 50,
  "cursor": null
}
```

### FR-06 搜索参数

| 参数 | 必填 | 说明 |
|---|---:|---|
| `source_ids` | 是 | 一个或多个已配置日志来源 |
| `keyword` | 是 | 待搜索的普通字符串 |
| `case_sensitive` | 否 | 是否区分大小写，默认 `false` |
| `start_time` | 否 | 查询开始时间 |
| `end_time` | 否 | 查询结束时间 |
| `order` | 否 | `oldest_first` 或 `newest_first` |
| `max_results` | 否 | 本次最大返回数量，不能突破服务端上限 |
| `cursor` | 否 | 用于继续查询后续结果 |

### FR-07 搜索语义

首期搜索规则：

- 支持 UTF-8 中文和英文。
- 使用字面量子串匹配。
- 行号从 1 开始。
- 空字符串不得作为查询关键字。
- 不将关键字解释为正则表达式。
- 不将关键字解释为通配符。
- 不将关键字解释为 Shell 命令或查询语言。
- `requestId`、`traceId`、异常名、错误码和业务 ID 均作为普通字符串搜索。
- 查询参数中的路径样式文本只作为普通搜索内容，不影响文件选择。

### FR-08 多日志来源查询

一次调用可以搜索多个已配置日志来源。

每条结果必须包含 `source_id`，以便 AI 判断日志来自哪个服务或环境。

首期不设计复杂的多来源状态模型。请求参数不合法、日志来源不存在或稳定配置路径无法读取时，直接返回 MCP 工具错误。

### FR-09 时间范围

搜索应支持可选的开始时间和结束时间。

行为规则：

1. 日志来源配置了时间戳解析规则时：
   - 尝试解析每条日志记录的时间戳。
   - 使用日志时间进行过滤和排序。
   - 结果中返回解析后的 `timestamp`。

2. 日志来源未配置时间戳解析规则时：
   - 可以使用文件修改时间缩小候选文件范围。
   - 不保证逐行时间过滤精度。
   - 结果中的 `timestamp` 可以为空。

3. 无法解析单行时间戳时：
   - 该结果的 `timestamp` 为空。
   - 不得伪造时间戳。

### FR-10 结果排序

支持：

```text
oldest_first
newest_first
```

当结果具有可解析时间戳时，应按时间排序。

无法解析跨来源时间戳时，应使用稳定、可重复的顺序，例如：

```text
日志来源顺序 → 文件顺序 → 行号顺序
```

不得将无法确定的结果伪装成精确的跨服务时间线。

### FR-11 搜索结果

建议响应结构：

```json
{
  "results": [
    {
      "match_ref": "match-7ac9",
      "source_id": "payment-test",
      "file_id": "file-91c2",
      "file_name": "application.log",
      "line_number": 1842,
      "timestamp": "2026-06-19T14:20:03.125+09:00",
      "content": "traceId=abc123 PaymentAuthException",
      "content_truncated": false
    }
  ],
  "truncated": false,
  "next_cursor": null
}
```

每条结果至少包含：

- `match_ref`
- `source_id`
- `file_id`
- `file_name`
- `line_number`
- `content`

可选包含：

- `timestamp`
- `content_truncated`

说明：

- `file_id` 和 `match_ref` 均为不透明标识。
- `file_name` 可以是文件名或日志来源内的相对显示名称。
- 不得返回服务器绝对路径。
- 日志行过长时可以截断，并设置 `content_truncated=true`。

### FR-12 无匹配结果

查询成功但没有匹配内容时，返回：

```json
{
  "results": [],
  "truncated": false,
  "next_cursor": null
}
```

无匹配不是工具错误。

### FR-13 结果截断和继续查询

当匹配结果超过本次返回上限或响应大小上限时：

- 返回当前批次结果。
- 设置 `truncated=true`。
- 在仍可继续查询时返回 `next_cursor`。

后续请求使用 `cursor` 获取下一批结果。

游标必须：

- 由服务端生成。
- 对客户端不透明。
- 不能被用于读取未配置日志。
- 具有服务端配置的有效期。
- 在查询条件不一致时拒绝使用。

---

## 8.4 查看日志上下文

### FR-14 `get_log_context`

MCP 应提供：

```text
get_log_context(...)
```

用于读取某个搜索匹配位置前后的有限日志行。

建议请求：

```json
{
  "match_ref": "match-7ac9",
  "before_lines": 10,
  "after_lines": 30
}
```

建议响应：

```json
{
  "source_id": "payment-test",
  "file_id": "file-91c2",
  "file_name": "application.log",
  "start_line": 1832,
  "end_line": 1872,
  "lines": [
    {
      "line_number": 1832,
      "content": "..."
    }
  ],
  "truncated": false
}
```

### FR-15 上下文读取约束

- 只能使用 `search_logs` 返回的 `match_ref`。
- 不能接受文件路径。
- 不能接受任意文件标识和任意行号组合。
- `before_lines` 和 `after_lines` 不能突破服务端上限。
- 上下文内容计入响应大小限制。
- 文件已经轮转、删除或引用失效时，返回 MCP 工具错误。
- 该能力用于读取匹配位置附近内容，不提供完整文件下载。

### FR-16 异常堆栈

首期不要求自动识别完整多行日志事件。

AI 可以通过 `get_log_context` 读取异常行之后的若干行，以获取：

- Java、Python 等语言的异常堆栈。
- `Caused by` 信息。
- 请求参数和下游响应。
- 重试过程和业务状态变更。

---

## 9. MCP 工具清单

首期提供三个工具：

| 工具 | 作用 |
|---|---|
| `list_log_sources` | 查询人工配置的日志来源 |
| `search_logs` | 按关键字搜索一个或多个日志来源 |
| `get_log_context` | 读取某个匹配位置前后的有限上下文 |

首期不提供：

- 任意文件读取工具。
- 目录浏览工具。
- Shell 命令工具。
- 完整日志下载工具。
- 实时 tail 或 follow 工具。
- 日志分析或根因判断工具。

---

## 10. 资源限制

服务端必须配置并强制执行以下限制：

| 限制项 | 说明 |
|---|---|
| 最大日志来源数量 | 单次查询允许选择的来源数量 |
| 最大关键字长度 | 防止异常大输入 |
| 最大扫描文件数 | 防止目录包含过多文件 |
| 最大扫描字节数 | 防止扫描过多历史日志 |
| 最大查询时间 | 超时后终止查询 |
| 最大返回结果数 | 限制单批结果数量 |
| 单条日志最大返回长度 | 防止超长单行日志占满响应 |
| 单次响应最大字节数 | 控制网络传输和 AI token 消耗 |
| 最大上下文行数 | 限制匹配位置前后读取范围 |
| 最大并发查询数 | 防止日志扫描占满磁盘 I/O |

建议首期默认值如下，部署时允许调整：

| 限制项 | 建议默认值 |
|---|---:|
| 单次最大日志来源数量 | 10 |
| 查询关键字最大长度 | 256 字符 |
| 单次最大扫描文件数 | 500 |
| 单次最大扫描字节数 | 512 MB |
| 单次查询最长时间 | 10 秒 |
| 单批最大返回结果数 | 200 |
| 默认返回结果数 | 50 |
| 单条内容最大返回长度 | 16 KB |
| 单次响应最大大小 | 1 MB |
| 单侧最大上下文行数 | 50 |

客户端传入的限制参数只能缩小范围，不能突破服务端硬限制。

---

## 11. 安全约束

系统必须满足：

1. 只能读取人工配置的日志来源。
2. AI 不能提交服务器文件路径或目录路径。
3. 禁止路径穿越。
4. 禁止通过软链接访问未授权文件。
5. 查询时不得跟随日志目录中的软链接。
6. 只能读取普通文件。
7. 禁止读取设备文件、FIFO、Socket 和其他特殊文件。
8. MCP 服务进程使用非 root 账号运行。
9. MCP 对日志文件和目录只拥有读取权限。
10. 禁止修改、删除、移动、创建或清空日志。
11. 禁止执行任意 Shell 命令。
12. 配置文件不得由 MCP 运行账号修改。
13. 错误信息不得返回服务器绝对路径、系统堆栈、服务器凭证或无关系统信息。
14. 日志内容被视为不可信数据，不得将日志中的文本当作 MCP 指令执行。
15. 自动敏感信息脱敏不属于首期范围；管理员应仅配置允许 AI 查询的日志来源，并确认日志内容适合在当前受控内网环境中返回。

路径安全校验不能只在服务启动时执行。实际打开文件时仍需确认文件位于配置范围内且为普通文件。

---

## 12. 部署要求

- MCP 服务部署在能够访问日志文件的服务器上。
- 服务仅在受控内网中提供访问。
- MCP 服务使用独立的非 root 运行账号。
- 日志文件和目录由人工配置。
- 日志目录建议以只读方式挂载给 MCP。
- AI 客户端通过 MCP 连接服务。
- AI 本地代码仓库不需要上传到日志服务器。
- 日志服务器不需要访问本地代码仓库。
- MCP 返回的结果必须限制大小。
- 配置更新失败时，不得破坏上一份有效配置。
- 首期可以通过重启服务加载配置，不强制要求热更新。

---

## 13. 非功能需求

### 13.1 稳定性

- 单个异常日志行不得导致服务进程退出。
- 日志在查询期间被追加、轮转、截断或删除时，服务进程不得崩溃。
- 查询达到时间、文件数或字节数限制后应及时停止。
- 客户端取消请求或断开连接后，应尽快停止对应扫描。
- 并发查询不得无限占用线程、内存或磁盘 I/O。
- 日志文件内容不完整或编码异常时，不得导致整个服务崩溃。

### 13.2 一致性

- 查询不锁定日志文件。
- 不承诺查询期间的完整快照。
- 对正在写入或轮转的日志采用尽力而为读取。
- 同一文件内结果按行号保持稳定顺序。
- 无法解析时间戳时，不承诺精确跨服务时间排序。

### 13.3 可观测性

服务自身至少应记录：

- 查询开始和结束时间。
- 查询使用的日志来源标识。
- 查询耗时。
- 扫描文件数和扫描字节数。
- 返回结果数。
- 是否触发资源限制。
- 工具执行错误。

为避免在 MCP 自身日志中再次泄露业务数据，默认不记录完整搜索关键字，可以记录关键字长度或哈希。

---

## 14. 边界说明

日志查询 MCP 不负责：

- 读取和分析本地代码仓库。
- 判断问题根因。
- 自动生成修复代码。
- 采集或同步日志。
- 管理日志轮转。
- 查询 Kubernetes API。
- 调用 `kubectl logs`。
- 查询 Loki、Elasticsearch 等日志平台。
- 执行服务器命令。
- 修改服务器文件。
- 持续订阅或实时 tail 日志。
- 自动识别和聚合完整多行日志事件。
- 提供完整日志文件下载。
- 支持正则表达式或复杂日志查询语言。
- 自动对日志内容进行敏感信息脱敏。
- 提供客户端认证、日志来源级授权或传输加密。

---

## 15. 首期验收标准

### 15.1 功能验收

1. MCP 服务可以部署在能够读取测试日志的服务器上。
2. 可以人工配置至少一个单文件日志来源和一个目录日志来源。
3. `list_log_sources` 能返回已启用日志来源的标识、名称、服务、环境和标签。
4. 日志来源列表不包含服务器绝对路径。
5. `search_logs` 能按 UTF-8 中文和英文普通字符串搜索日志。
6. 搜索内容不会被解释为正则表达式、通配符或 Shell 命令。
7. 能搜索单个日志来源。
8. 能同时搜索多个日志来源。
9. 能根据 `requestId`、`traceId`、异常名、错误码或业务 ID 找到匹配内容。
10. 每条搜索结果包含日志来源、文件标识、文件显示名称、从 1 开始的行号和匹配内容。
11. 配置时间戳解析规则后，能够按时间范围过滤并返回解析后的时间戳。
12. 未配置时间戳解析规则时，结果不伪造日志时间。
13. 能按 `oldest_first` 或 `newest_first` 返回可排序的结果。
14. 无匹配时返回空结果数组，不作为工具错误。
15. 结果超过上限时返回 `truncated=true`。
16. 仍有结果可继续获取时返回 `next_cursor`。
17. 使用游标能够获取下一批结果，且不能改变原查询条件。
18. `get_log_context` 能根据 `match_ref` 返回匹配位置前后的有限日志行。
19. 上下文结果包含行号，不返回服务器绝对路径。
20. 能通过上下文读取获得异常堆栈后续行。
21. 能查询当前日志和未压缩轮转日志。
22. 超长日志行会被截断，并明确标记。
23. 响应总大小不会突破服务端限制。

### 15.2 安全验收

24. 查询接口不接受任意服务器文件路径。
25. AI 无法查询未配置的文件和目录。
26. `../`、绝对路径和编码后的路径穿越内容不能改变查询文件范围。
27. 配置目录中的软链接不会被读取。
28. 普通文件在配置校验后被替换成软链接时，服务仍不会读取链接目标。
29. 设备文件、FIFO、Socket 和目录不会被当作日志文件读取。
30. MCP 运行账号不能修改、删除、移动或截断日志文件。
31. AI 无法通过任何工具参数执行 Shell 命令。
32. 错误响应不包含服务器绝对路径、系统堆栈或凭证。

### 15.3 稳定性验收

33. 查询过程中日志被追加时，服务进程不崩溃。
34. 查询过程中日志被轮转、截断或删除时，服务进程不崩溃。
35. 达到最大扫描文件数、扫描字节数或查询时间后，查询能够停止。
36. 达到并发查询上限后，服务不会继续无限增加资源占用。
37. 客户端取消请求后，服务能够停止对应扫描任务。
38. 日志中出现非法编码或超长单行时，服务进程不崩溃。

---

## 16. 后续可选能力

以下能力不属于首期，可根据实际使用效果再评估：

- JSON 结构化日志字段查询。
- 多关键字 `AND`、`OR` 和排除条件。
- 正则表达式搜索。
- 自动识别完整异常事件。
- 相同异常聚合和数量统计。
- 压缩日志查询。
- 实时 tail 或持续订阅。
- Loki、Elasticsearch 等日志平台适配器。
- Kubernetes 日志适配器。
- 自动敏感信息脱敏。
- 客户端认证和日志来源授权。

---

## 17. 核心定义

> 日志查询 MCP 是一个基于日志来源白名单的受控文本搜索服务，而不是远程文件浏览器或命令执行器。

人工负责配置服务器上的日志文件和目录；日志查询 MCP 负责安全地搜索日志并返回匹配位置和有限上下文；AI 结合本地代码仓库和日志搜索结果完成问题诊断。

首期核心能力是：

```text
日志来源发现
+ 普通字符串搜索
+ 时间范围
+ 多服务关联
+ 匹配上下文
+ 结果分页
+ 严格资源限制
```
